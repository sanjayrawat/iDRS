
import 'package:flutter/material.dart';

class ReplayHistoryScreen extends StatelessWidget {
  const ReplayHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Replay History")),
      body: const Center(child: Text("Replay history will appear here")),
    );
  }
}
