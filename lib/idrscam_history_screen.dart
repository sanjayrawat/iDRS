import 'package:flutter/material.dart';
class ReplayHistoryScreen extends StatelessWidget {
const ReplayHistoryScreen({super.key});
@override
Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Replay History')));
}